- make files shorter by moving helper functions or creating dirs and making code less obfuscated overall
- find ways to make RAM usage lower
- update modals which can use it with dropdown (new discord feature, can use dropdowns on modal e.g. for y/n question)
- update README.md


- 7w7:
    - implement: raid guide via reacts on raid menus
    - test: ticket refill notifier + setting on x tickets
    - implement: index 7w7 weapon abilities via reroll Screen indexing

- anigame:
    - implement:
        - grab all event series and add to array
        - fshop, cl shop etc wishlist setter via webhooks forwards to DM + 1 day forecast
        - compare talent + element card stats + market worth via DB
        - merge show gold cost of each card, and optionally order of their talent+element stats
        - market sell for lower than top via gold DB option for command giver
        - crit/evade notif
    - refactor: add market to index via /mk dex filters: -n wakana,marin,zoro,luffy too
    - fix: sr/ur emojis on claims not rendering

- karuta:
    - test: visit reminders
    - implement:
        - add visit and enhanced drop analysis to Quincy bot project
            - visit: 3d tilted > 2D Map > Streets Grey > template matching for car/burger emoji
            - drop: template matching by caching Fonts

- lab:
    - refractor: add emojis to menus e.g. feather, ✨ instead of "First Discovery"

- izzi:
    - implement: crate opening helper / extractor
    - implement: better talent lookup + emoji ID inserter for talents from ci

- dank:
    - fix: weekly bundle box reminders, if mel doesnt add himself
    - implement:
        Dank Alert Clone:
        Essentials:
            - Merchant Reminder
            - Happy Hour reminder
            - Scratch/Gboosts/fish events
            - Fishing RM
            - Prestige Calc

- utility:
    - implement: upload Quincy db periodically to discord via gzip'ing tables (json/yaml) as backup