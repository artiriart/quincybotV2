- copy quincybot cogs to V2
- make user cache less aggressive, or cache in DB (id -> username) myself

## Code functions which arent extract X message

- 7w7:
  - Junk drops autodelete setter (per server) + map out emoji -> Rarity (or emoji as it is rn)
  - presets viewer with pagnation and V2 components
- anigame:
  - clan/fshop reminder panel (to edit remind cards)
  -

- karuta:
  - drop recognition
  - visit reminders
  - card recognition
  - wishlist for card recog via series with script ENV setting: Gemma 1b / Tesseract / off

- lab:
  - reconstruct whole module to work better
  - Component V2 & better menus & home UI with all features
  - keep older logic in terms of core game logic

- utils:
  - DMuser function
  - user input numbers parser (for number user inputs)
  - custom reminder command

- dankmemer:
  - Levelling formula: 0.0000117058 x^2 + 0.0142744 x + 92.6934
  - harmony between multi XP & level calculator
  - store exact multi values in db so can use /multipliers xp user multis (user doesnt have to manually add)
  - coins / luck sources and also xp can be indexed via /dev (mostly for self curated description)
  - omega / prestige calculator
  - Nuke stat tracker + Component V2

## All bots:

- settings to toggle and filter by user
