- make files shorter by moving helper functions or creating dirs
- find ways to make RAM usage lower

- 7w7:
    - raid guide via reacts on raid menus
    - (test) ticket refill notifier + setting on x tickets

- anigame:
    - (test) cl/f reminder & fix claim

- karuta:
    - (test) visit reminders

- lab (low prio):
    - reconstruct whole module to work better
    - Component V2 & better menus & home UI with all features
    - keep older logic in terms of core game logic

- izzi:
    - raidlist
    - crate opening helper / extractor
    - index market via ci
    - (test) event shard extractor
    - fix: claim

- dank:
    - (fix) weekly bundle box reminders
    - (test) multiplier stats
    - (edit) do sync daily aka make it sync, not one time thing (sync = check which combinations not in table, fetch simulator if needed). also startup sync is ran daily too
    - for coins dont save Prestige 17 / omega stuff to dank_multipliers table too.
    - on /multipliers XP if page 1 add "record" reaction, will clean all XP multipliers,
      then wait for last page (add "done" reaction),
      then show all missing XP multipliers
    - redo trade reminder

- utility:
    - list all reminders command
    - setup github actions for push webhook notification
    - upload Quincy db periodically to discord via gzip'ing tables (json) as backup

